query vendors($eventId: ID!) {
  event(id: $eventId) {
    id
    vendors {
      id
      name
      vendorKind
      location
      contacts {
        contact {
          id
          name
          phone
          email
        }
        contactKind
      }
    }
  }
}