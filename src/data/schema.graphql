type Session {
  id: ID!
  isLoggedIn: Boolean
  error: String
}

type Query {
  session: Session!
}

type Mutation {
  signup(email: String!, password: String!): Session!
  login(email: String!, password: String!): Session!
  logout: Session!
}